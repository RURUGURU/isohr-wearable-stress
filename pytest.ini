[pytest]
minversion = 8.0
testpaths = scripts
python_files = test_*.py
pythonpath = .
addopts = -ra --strict-config --strict-markers
filterwarnings = error
markers =
    integration: exercises real checked-in datasets
